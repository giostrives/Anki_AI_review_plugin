[pytest]
testpaths = tests
; The repo root is itself a Python package (the add-on), so pytest's default
; importlib mode would import tests as Anki_AI_review_plugin.tests.* and pull
; in the add-on __init__.py. Classic prepend mode keeps test modules top-level.
addopts = --import-mode=prepend
